namespace ProjetoAW.Repositorio
{
    public class baseCRUD
    {
        /*Conexao cn = new Conexao();
        public void cadastra(model)
        {
            MySqlCommand cmd = new MySqlCommand("", cn.Conectar());
            cmd.Parameters.Add("", MySqlDbType.).Value = ;
            cmd.ExecuteNonQuery();

            cn.Desconectar();
        }

        public List<model> seleciona()
        {
            List<model> models = new List<model>();

            MySqlCommand cmd = new MySqlCommand("", cn.Conectar());
            MySqlDataReader dr = cmd.ExecuteReader();

            while (dr.Read())
            {
                models.Add(new model
                {

                });
            }

            cn.Desconectar();

            return models;
        }

        public model selecionaPorId(int id)
        {
            Model model = new Model();
            MySqlCommand cmd = new MySqlCommand("", cn.Conectar());
            cmd.Parameters.AddWithValue("");
            MySqlDataReader dr = cmd.ExecuteReader();

            while (dr.Read())
            {

            }

            cn.Desconectar();

            return model;
        }

        public void atualiza(model)
        {
            MySqlCommand cmd = new MySqlCommand("", cn.Conectar());
            cmd.Parameters.Add("", MySqlDbType.).Value = ;
            cmd.ExecuteNonQuery();
            cn.Desconectar();
        }

        public void exclui(int id)
        {
            MySqlCommand cmd = new MySqlCommand("", cn.Conectar());
            cmd.Parameters.AddWithValue("");
            cn.Desconectar();
        }*/
    }
}
